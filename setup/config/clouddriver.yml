server:
  port: 7002

redis:
  connection: redis://redis:6379

services:
  front50:
    baseUrl: http://front50:8080
  fiat:
    enabled: true
    baseUrl: http://fiat:7003

# Artifact configuration — required for CVE-2026-32604
artifacts:
  enabled: true
  http:
    enabled: true
    accounts:
      - name: test-http-account
        username: testuser
        password: supersecretpassword
  github:
    enabled: true
    accounts:
      - name: test-github-account
        token: ghp_testtoken123456789abcdef
  git-repo:
    enabled: true
    accounts:
      - name: test-git-repo
        username: gituser
        password: gitpassword

# Elasticsearch for entity tags
resilience4j.retry.instances.default:
  maxAttempts: 1

management.health.elasticsearch.enabled: false

swagger:
  enabled: true
  title: clouddriver

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
